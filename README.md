Тестовый проект Блога.

Start:

1. создать и заполнить `.env` по примеру `env.example`.
2. выполнить `./manage.py migrate`
3. выполнить `./manage.py runserver`
4. swagger можно увидеть по url:  `localhost:8000/redoc/`

